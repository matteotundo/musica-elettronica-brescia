import { defineConfig } from 'astro/config';

// Quando avrai il dominio, sostituisci qui l'indirizzo definitivo.
// Serve solo per generare la sitemap e i link assoluti.
export default defineConfig({
  site: 'https://musicaelettronica.it',
});
